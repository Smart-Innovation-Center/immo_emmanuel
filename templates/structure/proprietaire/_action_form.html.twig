<form action="{{ url('structure_proprietaire_delete', {id: user.id}) }}" method="post" data-confirmation="true" id="delete-form-{{ user.id }}">
	<a href="{{ url('structure_proprietaire_edit', {id: user.id}) }}" class="btn btn-sm btn-outline-primary">{{ 'Modifier' | trans }}</a>
	&nbsp;&nbsp;&nbsp;
	<a href="{{ url('structure_proprietaire_property', {id: user.id}) }}" class="btn btn-sm btn-outline-primary">{{ 'Afficher Les Biens' | trans }}</a>
	&nbsp;&nbsp;&nbsp;
	<input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
	<button type="submit" class="btn btn-sm btn-danger" onclick="if(confirm('{{ 'confirmation.delete_item' | trans }}')){ return true }else{ return false }">
		{{ 'Supprimer' | trans }}
	</button>
</form>
