<form action="{{ url('admin_gest_agence_cancel', {id: agence.id}) }}" method="post" data-confirmation="true" id="delete-form-{{ agence.id }}">
	<input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
	<button type="submit" class="btn btn-danger">
		{{ 'Annuler la création' | trans }}
	</button>
</form>
