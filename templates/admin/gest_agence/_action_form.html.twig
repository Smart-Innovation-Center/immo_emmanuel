<form action="{{ url('admin_gest_agence_val', {id: agence.id}) }}" method="post" data-confirmation="true" id="delete-form-{{ agence.id }}">
	<input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
	<button type="submit" class="btn btn-success">
		{{ 'Valider la création' | trans }}
	</button>
</form>
