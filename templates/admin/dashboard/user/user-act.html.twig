{% extends 'admin/layout/base.html.twig' %}

{% block title %}
	{{ 'dashboard'|trans }}
{% endblock %}

{% block body %}
	<div class="card mb-3">
		<div class="card-body">
			<div class="table-responsive">
				<table class="table" style="background-color: #FFFFFF">
					<thead class="thead">
						<th>Nom d'utilisateur</th>
						<th>Email</th>
						<th>Action</th>
						{#<tr>Role</tr>#}
					</thead>
					<tbody>
						{% for User_Act in User_Acts %}
							<tr>
								<td>{{User_Act.username}}</td>
								<td>{{User_Act.email}}</td>
								<td>
									<a href="{{ url('admin_block_user', {id: User_Act.id}) }}" class="btn btn-sm btn-outline-default" style="background-color:red; color:white">Désactiver</a>

								</td>
								{#<tr>{{tenant.roles | trans}}</tr>#}
							</tr>
						{% else %}
							<tr>
								<td>{{ 'no_items_found' | trans }}</td>
							</tr>
						{% endfor %}
					</tbody>
				</tbody>
			</table>
		</div>
	</div>

</div>{% endblock %}
